Skip to content
Navigation Menu
ily-snyk
dotNet-goof

Type / to search
Code
Pull requests
1
Actions
Projects
Wiki
Security
1
Insights
Settings
Full Snyk Security Workflow for .NET
Update main.yml #6
Jobs
Run details
Workflow file for this run
.github/workflows/main.yml at ffe3574
name: Full Snyk Security Workflow for .NET

on:
  push:
  pull_request:

jobs:
  security:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
          
      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj') }}
          restore-keys: |
            ${{ runner.os }}-nuget-
      - name: Restore dependencies
        run: dotnet restore

      - name: Build the project
        run: dotnet build --no-restore --configuration Release

      # Install Snyk CLI
      - name: Install Snyk CLI
        run: npm install -g snyk

      # Snyk Open Source Dependency Test
      - name: Snyk Open Source Test
        uses: snyk/actions/dotnet@master
        continue-on-error: true
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

      # Snyk Code (Static Application Security Testing)
      - name: Snyk Code Test
        run: snyk code test || true
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

      # Snyk Infrastructure as Code Test
      - name: Snyk IaC Test
        run: snyk iac test || true
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
Editing dotNet-goof/.github/workflows/main.yml at master · ily-snyk/dotNet-goof
